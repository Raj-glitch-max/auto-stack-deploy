{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "autostack-frontend.fullname" . }}
  labels:
    {{- include "autostack-frontend.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "autostack-frontend.selectorLabels" . | nindent 6 }}
{{- end }}
